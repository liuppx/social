spring:
  profiles:
    active: dev
  application:
    name: yeying-social-server

websocket:
  enable: true
  port: 8878

tcpsocket:
  enable: false # 暂时不开启
  port: 8879

jwt:
  accessToken:
    secret: MIIBIjANBgkq  # 跟platform的secret必须一致
